使用场景：和简单工厂方法类似；

与简单工厂方法的区别就是：
一个产品，一个工厂方法，如果新增一个产品，只需要新增一个类，去实线Send接口，再新增一个工厂方法，实线Factory接口即可。
不需要修改原来的逻辑；

优点：扩展性好；

特点：

一个抽象产品类（如Send接口），可以派生出多个具体产品类（如实现Send接口的SendMail与SendSms类）；

一个抽象工厂（如Factory接口），可以派生出多个具体工厂类（如实现Factory接口的MailFactory与SmsFactory）;

每个具体工厂类只能创建一个具体的产品实例；
